version: "3.7"
services:
  user-gql:
    image: 450680964076.dkr.ecr.ap-southeast-1.amazonaws.com/user-graphql:0.0.0
    networks:
      - cluster
    command: /go/bin/user-graphql
    env_file:
      - ./user-gql.env
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == worker
          - node.labels.group == web
      update_config:
        parallelism: 1
        delay: 5s
      labels:
        - "traefik.port=4003"
        - "traefik.enable=true"
        - "traefik.docker.network=forstok-ms_cluster"
        - "traefik.frontend.rule=Host:user-gql.forstok.com"
networks:
  cluster:
    driver: overlay
    attachable: true
